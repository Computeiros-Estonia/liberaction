from django.urls import path
from . import views

app_name = 'sales'

urlpatterns = [
    # path('cart/<int:pk>/', views.cart, name='cart'),
    # path('cart/<int:pk>/', views.product_details, name='product'),
]
